spring:
  application:
    name: main-service
  jpa:
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
---
spring:
  profiles:
    active: local
